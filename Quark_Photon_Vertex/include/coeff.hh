#pragma once
#include <math.h>

// your code
using namespace std

double a0 (int i)
{
 if (i==1)
  { 
   return sqrt(2);
  }
  
  else if (i==7 || i==10)
   {
    return 1;
   }
   
  else 
   {
    return 0;
   }
}
